import { MockupEmbed } from "../embed/MockupEmbed";

// Secretary — adopted from the mockup (index.html): split streaming chat + an
// open one-pager report on the right.
export default function DemoSecretary(): JSX.Element {
  return <MockupEmbed url="/demo-mockups/secretary.html" />;
}
