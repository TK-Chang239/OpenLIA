import { MockupEmbed } from "../embed/MockupEmbed";

// Repository — adopted from the mockup (repository.html): refined saved-reports
// list with a Filters drawer, active-filter chips, and page-count badges.
export default function DemoRepository(): JSX.Element {
  return <MockupEmbed url="/demo-mockups/repository.html" />;
}
