interface Props {
  variant: "chip" | "header";
  reportId: string;
  filename: string;
}

export function SaveToRepoButton({ variant: _variant, reportId: _reportId, filename: _filename }: Props): JSX.Element {
  return <></>;
}
